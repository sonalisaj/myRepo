1]
-   cd myproject :
    To begin, open up a terminal and move to where you want to 
    place the project on your local machine using the cd 
    (change directory) command.
-  git init :
    To initialize a git repository in the root of the folder

2]
 -  git status :
     command to see which files git knows exist
 -  git add <filename> :
    To add a file to a commit, you first need to add it to the staging environment
 -  git commit -m "Your message about the commit" :
    Once you've used the git add command to add all the files you want to the 
    staging environment, you can then tell git to package them into a commit 
    using the git commit command


